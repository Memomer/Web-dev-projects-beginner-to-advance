### Download the files and save as `mario-game` in any desired directory

### Find the `mario-game` folder and open it in your terminal

### Install the webpack packages using `npm install`

### To run the game, run `npm start`
